# Template for the gitignored .env — copy to `.env` and fill in real values.
#   cp .env.example .env
# Load before running incident-doc commands: `set -a; source .env; set +a`

# Sudo password for VM 109 "Main" guest (ssh proxmox_main / user proxmox-ml5).
PROXMOX_MAIN_SUDO_PW=
